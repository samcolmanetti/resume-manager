# Resume content (your .tex files live here, never tracked)
resumes/

# Build aux (LaTeX intermediate files)
.build-aux/

# Per-user export config (copy config.local.mk.example -> config.local.mk)
config.local.mk

# Local agent/tooling run state
.context/

.DS_Store

# Skills installer lock (local tooling state, not committed)
skills-lock.json
